name: deb

on:
  workflow_dispatch:

jobs:
  d0ck3r:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Docker Login
        uses: docker/login-action@v3.6.0
        with:
          username: ${{ secrets.DOCHUB_USER }}
          password: ${{ secrets.DOCHUB_PASS }}
             
      - name: pull the image
        run : |
          docker run -d \
          --name=1m4g3 \
          r014nd5m1th/r3m1x:deb
          
      - name: ls commit and tag the image
        run : |
          docker images
          docker commit 1m4g3 rem1x:1m4g3
          docker tag rem1x:1m4g3  r014nd5m1th/k45mt0p:deb
         
      - name: Push the image
        run : |
          docker push r014nd5m1th/k45mt0p:deb
